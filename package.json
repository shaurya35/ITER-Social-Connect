{
    "name": "Campus Connect",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
  
      "push:main": "git add . && git commit -m \"Minor fixes\" && git push origin main",
      "push:production": "git add . && git commit -m \"Minor fixes\" && git push origin production"
    }
  }
  