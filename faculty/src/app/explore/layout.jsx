export const metadata = {
  title: "Admin / ITER Connect",
  description: "Connect, Collaborate, and Grow",
};

const ExploreLayout = (props) => {
  return <div>{props.children}</div>;
};

export default ExploreLayout;
