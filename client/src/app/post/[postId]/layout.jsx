export const metadata = {
  title: "My Posts / ITER Connect",
  description: "Connect, Collaborate, and Grow",
};

const PostsLayout = (props) => {
  return <div>{props.children}</div>;
};

export default PostsLayout;
